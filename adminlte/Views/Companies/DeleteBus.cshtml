@model adminlte.Models.Bus

@{
    ViewBag.Title = "DeleteBus";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="content-wrapper">
    <h2>DeleteBus</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Bus</h4>
        <hr />
        <dl class="dl-horizontal">


            <dt>
                @Html.DisplayNameFor(model => model.BusModel)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.BusModel)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.BusPlate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.BusPlate)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.BusColor)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.BusColor)
            </dd>


        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "ManageBuses")
            </div>
        }
    </div>
    </div>